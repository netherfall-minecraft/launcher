export global Theme {
  in property <brush> bg:           #0f1218;
  in property <brush> surface:      #171b24;
  in property <brush> surfaceAlt:   #1b202a;
  in property <brush> primary:      #4C7CF3;
  in property <brush> primaryHover: #5a88f5;
  in property <brush> text:         #E8E9ED;
  in property <brush> textMuted:    #AAB0BD;
  in property <length> radius:      12px;
}
